<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android07">

    <application
        android:allowBackup="true"
        android:name="com.example.android07.chess.MyApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="The Offlaners REAL Game of Chess"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AboutUs"
            android:label="About the Creators" />
        <activity android:name=".ReplayGame"
            android:label="Replays" />
        <activity android:name=".ChessGame"
            android:label="Chess"
            />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>